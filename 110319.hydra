src(s0)
  .diff(shape(5,({time}) => Math.sin(time*3)*3 + 5,2).invert(({time})=>Math.sin(time/7)*0.1))
  .diff(shape(10,({time}) => Math.sin(time*2)*3 + 10,2).invert(({time})=>Math.sin(time/8)*0.15))
  .diff(shape(8,({time}) => Math.sin(time*1.3)*4 + 12,2).invert(({time})=>Math.sin(time/9)*0.11))
  .diff(shape(3,({time}) => Math.sin(time*2.1)*2 + 3,2).invert(({time})=>Math.sin(time/10)*0.13))
  .modulateKaleid(osc(0.5))
  .colorama(0.005)
  .rotate(({time}) => time/10)
  .out()
